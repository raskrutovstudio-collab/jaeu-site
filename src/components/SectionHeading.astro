---
interface Props {
  title: string;
  lead?: string;
}

const { title, lead } = Astro.props;
---

<div class="section-heading">
  <h2>{title}</h2>
  {lead && <p class="section-heading__lead">{lead}</p>}
</div>
